/**
 * Wave 3 — Theme switching on the Settings → Appearance tab.
 */
import { test } from './fixtures';

test.describe('Feature: Theme switching', () => {
  test.describe('Given the Settings panel is open on the Appearance tab', () => {
    test.fixme(
      'When the user clicks a theme card, Then settings.update is called with that themeId',
      async () => {
        // Implement.
      },
    );

    test.fixme(
      'When the user picks two themes in succession, Then both updates fire and the active card indicator follows the latest pick',
      async () => {
        // Implement.
      },
    );
  });
});
