/**
 * Wave 2 — Landing screen empty state.
 *
 * Bodies are intentionally unimplemented (test.fixme). The describe
 * blocks pin the GWT contract; implementations land in a follow-up.
 */
import { test } from './fixtures';

test.describe('Feature: Landing screen, empty state', () => {
  test.describe('Given a user with no recent projects', () => {
    test.fixme(
      'When the page boots, Then empty-state copy + "New project" + "Open project from disk" CTAs are visible',
      async () => {
        // Implement.
      },
    );

    test.fixme(
      'When the user clicks the Settings cog, Then the Settings panel opens',
      async () => {
        // Implement.
      },
    );
  });
});
