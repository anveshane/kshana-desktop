/**
 * Wave 5 — Cancel a running task mid-flight.
 */
import { test } from './fixtures';

test.describe('Feature: Cancel running task', () => {
  test.describe('Given a long-running scenario where events fire over a delay', () => {
    test.fixme(
      'When the user clicks Cancel mid-flight, Then cancelTask is called and the UI returns to idle (Send re-enabled, Cancel hidden)',
      async () => {
        // Implement — reuse edit-with-streaming.json or create a longer-running scenario.
      },
    );
  });
});
